#!/bin/bash
# /storage/roms/ports/Disable MinUI Amber.sh
# Restores EmulationStation as the boot front end and reboots. Nothing is
# deleted, so re-enabling later is one more Ports entry away.
exec /storage/roms/MinUIAmber/EnableMinUIAmber.sh off
