#!/bin/bash
# /storage/roms/ports/Enable MinUI Amber.sh
# Shows up in EmulationStation under Ports. Run it once to switch the
# device over to MinUI; the device reboots into MinUI when it finishes.
exec /storage/roms/MinUIAmber/EnableMinUIAmber.sh on
